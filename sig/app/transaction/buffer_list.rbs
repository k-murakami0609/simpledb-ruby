# TypeProf 0.21.8

# Classes
class BufferList
  @buffers: Hash[BlockId, Buffer]
  @pins: Array[BlockId]
  @buffer_manager: BufferManager

  def initialize: (BufferManager buffer_manager) -> void
  def get_buffer: (BlockId block_id) -> Buffer?
  def pin: (BlockId block_id) -> Array[untyped]
  def unpin: (BlockId block_id) -> nil
  def unpin_all: -> Array[untyped]
end
